sudo: required
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gfortran-7
    - gcc-7
language: cpp
install:
  - ./install-ompi.sh
script:
  - cd Build/mpi_gnu_linux_64
  - ./make_fds.sh
