# fds2ascii -- a conversion tool for FDS output

`fds2ascii.f90` is a Fortran program that reads FDS slice (`.sf`), boundary (`.bf`), and Plot3D (`.q`) files. There is a makefile and folders used to build the program for different operating systems.
