To remake species table copy THERMO_DATA_TYPE and
SUBROUTINE DEFINE_THERMO_PROPS from prop.f90 and
replace the routines in make_spec_table.f90.

The resulting species table may require reordering. Current
table is sorted by chemical formula.