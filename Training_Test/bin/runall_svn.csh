#!/bin/csh -f

# This script, runall_svn.csh, is a template under svn control.
# It contains entries for each test case to be run.  This script
# will be modified as test cases are added or removed.
# To use this script:

#  1.  copy runall_svn.csh to runall.csh  (only when runall_svn.csh changes)
#  2.  Edit runall.csh by changing hostnames on each runfds.csh line 
